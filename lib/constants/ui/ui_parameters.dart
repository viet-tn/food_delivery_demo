import 'package:flutter/material.dart';

class Ui {
  static const _padding = 15.0;
  static const borderRadius = BorderRadius.all(Radius.circular(15.0));
  static const screenPadding =
      EdgeInsets.fromLTRB(_padding, _padding, _padding, 0.0);
  static const screenPaddingHorizontal =
      EdgeInsets.symmetric(horizontal: _padding);
}
